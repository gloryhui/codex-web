# Codex Web 自定义调整索引

这份索引记录**当前仍需保留或在重新提取 Desktop Web 资源时重新核对**的 Web 适配，包括本 fork 的修复和从 codex-web 原项目继承、但仍由本仓库承担的浏览器功能。它是升级检查清单，不是某次压缩包的行号清单。当前基线：Desktop `26.924.20706`（[`default.nix`](default.nix)、[`scripts/prepare`](scripts/prepare)），CLI 固定 `0.157.1`（[`nix/codex/default.nix`](nix/codex/default.nix)、[`Dockerfile`](Dockerfile)）。提取新版界面不自动升级 CLI；只有后续出现需要新版 CLI 的较大变化，才单独评估和修改锁定版本。

`patches/codex-web-browser.patch` 是对当次提取包的精确差异，文件名和压缩代码位置会随版本变化；`scripts/patch_codex_web_bundle.mjs`、`scripts/patch_codex_web_renderer.mjs` 是按特征查找的动态补丁；`src/` 是持续保留的浏览器和服务端适配。下表的“搜索锚点”用于在新包里重新定位，不能仅凭旧补丁可以应用就认定行为正确。

| ID | 应保留的行为 / 曾出现的故障 | 代码索引与搜索锚点 | 每次提取后的核对 |
| --- | --- | --- | --- |
| WEB-01 启动桥接 | 页面能进入会话，刷新和重连后不无限加载；新版 Desktop 的 WSL setter、`NativeIntl`、主渲染窗口初始化不能中断启动。 | [`scripts/patch_codex_web_bundle.mjs`](scripts/patch_codex_web_bundle.mjs) 中 `shouldSpawnInsideWsl`、`NativeIntl has not been configured`、`setRendererWindowFactory`；[`src/server/main.ts`](src/server/main.ts) 中 `rendererWindowFactory`；[`src/server/electron/index.ts`](src/server/electron/index.ts) 中 `createPrimaryWindow`。 | 动态补丁匹配数量正确，启动后进入会话、刷新、断线重连均成功；匹配失败要先检查新版原始实现。 |
| WEB-02 Electron API 兼容 | Web 宿主不会因 Desktop 新调用的 Electron API 缺失而在启动时抛错。 | [`src/server/electron/index.ts`](src/server/electron/index.ts) 中 `app.dock`、`screen.isCursorScreenPointSupported`、`setPermissionPromptHandler`、`setWebsiteReportingEnabled`；[`src/browser/shim.ts`](src/browser/shim.ts) 中 `get-initial-sidebar-bootstrap`。 | 查看启动日志及浏览器控制台；对照新版调用契约验证 stub 返回值，不能只加空函数掩盖必需行为。 |
| WEB-03 多标签 IPC | 同一 Web 服务打开多个标签时，各标签的会话和消息端口独立；旧标签不因“primary renderer 被替换”失效。 | [`patches/codex-web-browser.patch`](patches/codex-web-browser.patch) 搜 `Primary renderer was replaced`、`Renderer was destroyed`、`structuredClonable`/`jsonCompatible`；[`src/server/main.ts`](src/server/main.ts) 搜 `rendererSockets`、`WebSocketMessagePort`；[`src/browser/shim.ts`](src/browser/shim.ts) 搜 `ensureSocket`、`postMessage`。 | 同时打开两个标签，各自切换会话、发消息，再关闭/刷新其中一个；确认另一个仍可用，MessagePort 和 IPC 不串线。 |
| WEB-04 远程工作区 | 浏览器可选择**服务端**目录，也可输入绝对路径；新版和旧版工作区选择消息都能处理，取消不会误选。 | [`src/browser/shim.ts`](src/browser/shim.ts) 中 `isPickWorkspaceRootOptionMessage`、`isUnhandledAddWorkspaceRootOptionMessage`、`selectAndEmitWorkspaceRootPicked`、`selectAndSendWorkspaceRoot`；[`src/browser/workspace-root-dialog.tsx`](src/browser/workspace-root-dialog.tsx) 中 `handleSubmit`、`HOST_EVENT_NAMES`；[`src/server/main.ts`](src/server/main.ts) 中 `getWorkspaceDirectoryEntries`。 | 分别试目录浏览、直接输入路径、取消、错误路径；选中后的根目录应是服务端路径，弹层点击不穿透到原界面。 |
| WEB-05 中文默认开启 | Desktop 的国际化开关关闭时，Web 仍默认显示中文本地化。 | [`src/browser/shim.ts`](src/browser/shim.ts) 中 `getDynamicConfigOverride`、`72216192`、`enable_i18n`；[`patches/codex-web-browser.patch`](patches/codex-web-browser.patch) 搜 `overrideAdapter`、`loggingEnabled`。 | 打开首页、设置和会话，确认应本地化的文案显示中文；若配置键或 Statsig 接入变化，重新定位真正的开关。 |
| WEB-06 侧栏会话操作 | 浏览器侧栏的会话行可打开操作菜单，重命名等菜单项可执行。 | [`scripts/patch_codex_web_renderer.mjs`](scripts/patch_codex_web_renderer.mjs) 中 `sidebarSurfaceGates`；[`src/browser/shim.ts`](src/browser/shim.ts) 中 `showContextMenu`。 | 在桌面宽度和手机宽度分别点侧栏会话操作；验证菜单显示、操作生效、关闭行为。 |
| WEB-07 H5 视口与滚动 | 手机/H5 底部输入框完整可见；左侧列表可手指上下拖动，打开软键盘后仍可访问输入框。 | [`src/browser/shim.ts`](src/browser/shim.ts) 中 `syncMobileViewportHeight`、`--codex-web-viewport-height`；[`patches/codex-web-browser.patch`](patches/codex-web-browser.patch) 的 `webview/index.html` hunk，搜 `touch-action: pan-y`、`data-app-action-sidebar-scroll`。 | 真机或移动浏览器窄屏测试：打开/收起键盘、横竖屏、长列表滚动、点选会话，观察输入框底部及侧栏触控。 |
| WEB-08 移动端编辑器 | 触摸或鼠标点击后可正常激活输入框和软键盘；失焦后输入模式恢复。 | [`patches/codex-web-browser.patch`](patches/codex-web-browser.patch) 的 `app-initial` hunk，搜 `inputmode`、`setPointerInputMode`、`touchstart`。 | 手机触摸输入、失焦再聚焦；桌面鼠标点击输入。与 WEB-07 一起回归。 |
| WEB-09 浏览器路由、分享和标题 | `/thread/<id>` 深链可进入对应会话、前进后退可用；系统分享内容填入新会话；浏览器标签标题随会话变化。 | [`src/browser/routes.ts`](src/browser/routes.ts) 中 `mapBrowserPathToInitialRoute`、`dispatchNavigateToRoute`；[`src/browser/shim.ts`](src/browser/shim.ts) 中 `onMemoryNavigationChanged`、`currentSearch`；[`patches/codex-web-browser.patch`](patches/codex-web-browser.patch) 搜 `initialEntries`、`prefillPrompt`、`document.title`。 | 直接访问深链、刷新、前进后退；访问 `/share/receive?title=...&text=...`，确认草稿出现且未误发送；切换会话看标题。 |
| WEB-10 Web 资源加载 | 提取的 Electron 页面能通过浏览器加载 preload、静态资源和 PWA 元数据；头部不留 Desktop 专属左空白。 | [`patches/codex-web-browser.patch`](patches/codex-web-browser.patch) 的 `webview/index.html` hunk，搜 `Content-Security-Policy`、`<base href`、`preload.js`、`manifest.json`、`spacing-token-safe-header-left`；[`scripts/prepare_asar`](scripts/prepare_asar) 中 `PWA_SOURCE_ICON`。 | 首页资源无 404、无 CSP 阻断，刷新深链后样式正常；检查图标和顶部布局。 |
| WEB-11 宿主环境适配 | 浏览器宿主不尝试 Desktop 专用设备校验及 Sentry 初始化。 | [`patches/codex-web-browser.patch`](patches/codex-web-browser.patch) 搜 `isDeviceCheckSupported`、`enabled: !1`；[`src/browser/shim.ts`](src/browser/shim.ts) 中 `get-sentry-init-options`。 | 启动和进入会话无设备校验阻塞、Sentry 初始化错误；若新版入口改变，核实实际调用链。 |
| WEB-12 版本锁定 | 提取界面与 CLI 版本分别管理；当前 CLI 为 `0.157.1`。 | [`default.nix`](default.nix) 的 `appVersion`、[`scripts/prepare`](scripts/prepare) 的 `APP_VERSION`；[`nix/codex/default.nix`](nix/codex/default.nix) 的 `version`、[`Dockerfile`](Dockerfile) 的 `CODEX_CLI_VERSION`。 | 提取后核对 Desktop 版本一致，Nix/Docker CLI 仍为锁定版本；只有明确决定升级 CLI 时才同时修改两处并验证。 |
| WEB-13 本地附件 | 浏览器选择单个/多个文件后可上传到 Web 服务端，返回可供 Desktop 会话引用的路径；取消选择不留下错误附件。 | [`src/browser/files.ts`](src/browser/files.ts) 中 `isLocalFilePickerMessage`、`openBrowserFilePicker`、`uploadFiles`、`sendFetchResponse`；[`src/browser/shim.ts`](src/browser/shim.ts) 中 `handleLocalFilePickerMessage`；[`src/server/main.ts`](src/server/main.ts) 中 `/__backend/upload`。 | 上传图片及普通文件、多个文件，取消一次选择；确认会话能访问附件，不把客户端本机路径误当服务端路径。 |
| WEB-14 外部链接 | Desktop 发送的 `open-in-browser` 消息在浏览器打开目标地址。 | [`src/browser/shim.ts`](src/browser/shim.ts) 中 `isOpenInBrowserMessage` 和 `window.open`。 | 从会话中打开外部链接，确认新标签目标正确，原会话保留。 |
| WEB-15 Web 功能门控 | 浏览器所需的两个 Statsig gate override 继续按当前语义生效，尤其远程控制入口；不能因重提取丢失接线。 | [`src/browser/shim.ts`](src/browser/shim.ts) 中 `getGateOverride`、`2911712394`、`1042620455`；[`patches/codex-web-browser.patch`](patches/codex-web-browser.patch) 中 `overrideAdapter`。 | 对照新版未修改包识别 gate 的实际用途，再检查 Web 端对应入口与可用状态；若 gate ID/用途变了，不能沿用旧编号盲目开启。 |

## 每次提取的处理规则

1. 在修改补丁前保留“旧版已修复”和“新版未经修改”两份提取结果；对照本表逐项找新版对应实现。参照 [`UPGRADING.md`](UPGRADING.md) 用 `diff` 从新版本未修改目录重新生成补丁，不手写压缩包差异。动态补丁也要核对匹配数量和实际语义。
2. 对每个 ID 记录结果：**仍需迁移**、**新版已原生实现**、**待验证**。补丁无法匹配、函数改名或界面可打开，都不足以判定“已实现”。尤其 WEB-07 要在移动端验证，WEB-03 要用两个标签验证。
3. 只有在**未应用本仓库对应补丁/适配的新版提取结果**中确认同一行为存在，并在浏览器完成该项回归后，才能删除相应补丁、动态匹配或 shim 分支，再重新提取、构建、验证，最后从本索引移除该 ID。若同一代码同时服务其他 ID，只删除不再需要的部分；避免新旧实现并存。
4. 把全部 ID 的核对结果和发现的新故障留在当次升级记录中；部署或推送是独立操作，不属于提取及核对流程。

`0dfdc10` 已从 codex-web 上游合入浏览器标签隔离和 app-host 重连，因此它不是一项待重新移植的本地补丁；WEB-03 记录的是当前 Desktop 提取包与 Web 桥接仍需兼容的具体行为。
