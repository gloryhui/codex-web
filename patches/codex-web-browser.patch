--- a/.vite/build/main-BefHSPFJ.js
+++ b/.vite/build/main-BefHSPFJ.js
@@ -176443,25 +176443,17 @@
       if (this.startupReady != null) {
         let t = Promise.withResolvers(),
           n = () => {
-            t.reject(
-              new DOMException(`Primary renderer was replaced`, `AbortError`),
-            );
-          },
-          r = this.windowManager.subscribePrimaryWindowChanges((e) => {
-            e?.webContents.id !== this.origin.id && n();
-          });
+            t.reject(new DOMException(`Renderer was destroyed`, `AbortError`));
+          };
         this.origin.once(`destroyed`, n);
         try {
           e = await Promise.race([this.startupReady, t.promise]);
         } finally {
-          (r(), this.origin.off(`destroyed`, n));
+          this.origin.off(`destroyed`, n);
         }
       }
-      if (
-        this.isDisposed ||
-        this.windowManager.getPrimaryWindow()?.webContents.id !== this.origin.id
-      )
-        throw new DOMException(`Primary renderer was replaced`, `AbortError`);
+      if (this.origin.isDestroyed())
+        throw new DOMException(`Renderer was destroyed`, `AbortError`);
       return this.includeStartupTiming ? e : void 0;
     }
     isSentryEnabled() {
@@ -185989,7 +185981,7 @@
   return new o.yt(r, t, n).getRemoteMain();
 }
 var xit = class {
-    encodingLevel = `structuredClonable`;
+    encodingLevel = `jsonCompatible`;
     constructor(e) {
       ((this.#e = e),
         e.start(),
--- a/.vite/build/preload.js
+++ b/.vite/build/preload.js
@@ -206,8 +206,7 @@
     getDesktopUserAgent: () => C({ appVersion: O.appVersion }),
     getAppSessionId: () => O.codexAppSessionId,
     getBuildFlavor: () => k,
-    isDeviceCheckSupported: () =>
-      process.platform === `darwin` && process.arch === `arm64`,
+    isDeviceCheckSupported: () => false,
     isIntelMacBuild: () =>
       process.platform === `darwin` && process.arch === `x64`,
   };
--- a/.vite/build/worker.js
+++ b/.vite/build/worker.js
@@ -113582,6 +113582,7 @@
   let r = uF(e.appVersion),
     i = e.buildFlavor !== `prod`;
   (rx({
+    enabled: !1,
     dsn: e.dsn,
     transport: (e) => {
       let t = uu(e.bufferSize ?? 64);
--- a/webview/assets/app-initial-0a6dd402dd72.js
+++ b/webview/assets/app-initial-0a6dd402dd72.js
@@ -51978,6 +51978,7 @@
     n = Iwe(e.appVersion),
     r = e.buildFlavor !== `prod`;
   (Rht({
+    enabled: !1,
     beforeBreadcrumb: rre,
     beforeSend: Qee,
     dsn: e.dsn,
@@ -108244,7 +108245,7 @@
         "--app-shell-root-banner-height": ae,
         "--app-shell-navigation-rail-width": $e,
         "--app-shell-application-menu-height": et,
-        "--spacing-token-safe-header-left": tt,
+        "--spacing-token-safe-header-left": `0px`,
         "--spacing-token-safe-header-right": nt,
         "--app-shell-animated-left-panel-width": void 0,
         width: `calc(100vw / var(--codex-window-zoom))`,
@@ -229848,10 +229849,23 @@
     i;
   t[0] === r
     ? (i = t[1])
-    : ((i = () =>
-        nae([{ path: `*`, Component: iri, ErrorBoundary: ari }], {
-          initialEntries: r,
-        })),
+    : ((i = () => {
+        let router = nae([{ path: `*`, Component: iri, ErrorBoundary: ari }], {
+          initialEntries: r ?? [window.__ELECTRON_SHIM__?.initialRoute ?? `/`],
+        });
+        let lastKey = router.state.location.key;
+        if (window.__ELECTRON_SHIM__?.onMemoryNavigationChanged)
+          router.subscribe((state) => {
+            if (state.location.key === lastKey) return;
+            lastKey = state.location.key;
+            window.__ELECTRON_SHIM__?.onMemoryNavigationChanged?.({
+              action: state.historyAction,
+              location: state.location,
+              delta: 0,
+            });
+          });
+        return router;
+      }),
       (t[0] = r),
       (t[1] = i));
   let [a] = (0, sri.useState)(i),
@@ -346594,6 +346608,8 @@
 function OTa(e) {
   let t = {
     ...zTa,
+    overrideAdapter: window.__ELECTRON_SHIM__?.overrideAdapter,
+    loggingEnabled: `disabled`,
     enableLiveValuesAutoRefresh: !0,
     ...(e === !0 ? {} : { disableStorage: !0, loggingEnabled: `disabled` }),
   };
@@ -402885,13 +402901,21 @@
   }
   I != null && u != null && !f && (I = LVn(I, u, e ?? void 0));
   let L = u == null ? null : Quo(f ? u : null, (e) => Huo(v, e)),
+    inputModeDisabled = !1,
+    getAttributes = () => ({
+      "aria-multiline": `true`,
+      dir: `auto`,
+      role: `textbox`,
+      spellcheck: `true`,
+      ...(inputModeDisabled ? {} : { inputmode: `none` }),
+    }),
+    setPointerInputMode = (e) => {
+      inputModeDisabled !== e &&
+        ((inputModeDisabled = e),
+        R.isDestroyed || R.setProps({ attributes: getAttributes }));
+    },
     R = new rDe(null, {
-      attributes: {
-        "aria-multiline": `true`,
-        dir: `auto`,
-        role: `textbox`,
-        spellcheck: `true`,
-      },
+      attributes: getAttributes,
       transformPastedText: k.transformPastedText,
       transformPastedHTML(e) {
         return (
@@ -403050,7 +403074,9 @@
       },
       handleDOMEvents: {
         keydown: (e, t) => k.handleKeyEvent(t),
-        mousedown: (e, t) => k.handleMouseDown(t),
+        mousedown: (e, t) => (setPointerInputMode(!0), k.handleMouseDown(t)),
+        touchstart: () => (setPointerInputMode(!0), !1),
+        blur: () => (setPointerInputMode(!1), !1),
         keyup(e, t) {
           k.handleKeyEvent(t);
           let { $from: n, empty: r } = e.state.selection;
--- a/webview/assets/app-primary-620c47f764fe.js
+++ b/webview/assets/app-primary-620c47f764fe.js
@@ -148712,7 +148712,7 @@
       setComments: s,
     } = e,
     c = Cm(M),
-    l = o?.prefillPrompt,
+    l = o?.prefillPrompt ?? new URLSearchParams(window.__ELECTRON_SHIM__?.currentSearch ?? window.location.search).get(`prompt`),
     u = o?.prefillPromptFormat,
     d = o?.prefillPromptOnlyIfEmpty,
     f = o?.prefillLibraryFolders,
@@ -162695,7 +162695,7 @@
     ue = (0, w9.useRef)(null);
   (c.get(IE, le), Q(tC, le));
   let de = String(D?.focusComposerNonce ?? w),
-    fe = D?.prefillPrompt,
+    fe = D?.prefillPrompt ?? new URLSearchParams(window.__ELECTRON_SHIM__?.currentSearch ?? window.location.search).get(`prompt`),
     pe = D?.prefillPromptFormat,
     me = D?.prefillPromptOnlyIfEmpty,
     he = D?.prefillLibraryFolders,
@@ -170264,9 +170264,9 @@
     );
   }
   let G =
-      g?.prefillPromptOnlyIfEmpty && g.prefillPrompt != null
+      (g?.prefillPromptOnlyIfEmpty && g.prefillPrompt != null
         ? void 0
-        : (g?.prefillPrompt ?? void 0),
+        : (g?.prefillPrompt ?? void 0)) ?? new URLSearchParams(window.__ELECTRON_SHIM__?.currentSearch ?? window.location.search).get(`prompt`),
     K;
   t[23] === m ? (K = t[24]) : ((K = Gd(m)), (t[23] = m), (t[24] = K));
   let q = K,
--- a/webview/assets/local-conversation-thread-45d4762b12ed.js
+++ b/webview/assets/local-conversation-thread-45d4762b12ed.js
@@ -3306,6 +3306,10 @@
   let E = Z(lg, C, T),
     D = Z(null, i ? h : null) ?? [];
   (Z(null, h, { enabled: !1 })?.data, Z(null, null));
+  (0, r().useEffect)(() => {
+    let title = typeof v === `string` ? v.trim() : ``;
+    title && (document.title = `${title} | Codex`);
+  }, [v]);
   let O = E,
     k = D,
     A = ui() === Fr,
--- a/webview/index.html
+++ b/webview/index.html
@@ -3,13 +3,13 @@
   <head>
     <meta name="referrer" content="origin" />
     <!-- PROD_BASE_TAG_HERE -->
-    <meta
-      http-equiv="Content-Security-Policy"
-      content="default-src &#39;none&#39;; img-src &#39;self&#39; app: blob: data: https:; child-src &#39;self&#39; blob: codex-sandbox://*.web-sandbox.oaiusercontent.com codex-sandbox://web-sandbox.oaiusercontent.com https://*.web-sandbox.oaiusercontent.com https://web-sandbox.oaiusercontent.com; frame-src &#39;self&#39; blob: codex-sandbox://*.web-sandbox.oaiusercontent.com codex-sandbox://web-sandbox.oaiusercontent.com https://*.web-sandbox.oaiusercontent.com https://cdn.plaid.com https://cdn.platform.openai.com https://mfe.prod.us-exp-api.experiancs.com https://mfe.uat.us-exp-api.experiancs.com https://web-sandbox.oaiusercontent.com https://www.youtube.com; worker-src &#39;self&#39; blob:; script-src &#39;self&#39; &#39;sha256-Z2/iFzh9VMlVkEOar1f/oSHWwQk3ve1qk/C2WdsC4Xk=&#39; &#39;wasm-unsafe-eval&#39; https://cdn.plaid.com/link/v2/stable/link-initialize.js https://persistent.oaistatic.com/voice/silero-vad-v6/ort-wasm-simd-threaded.30dd851d9c00622940500f71ddd2ff8820c5cb65270816080175b958705385a8.module.js; style-src &#39;self&#39; &#39;unsafe-inline&#39;; font-src &#39;self&#39; data:; media-src &#39;self&#39; app: blob: data: https:; connect-src &#39;self&#39; https://ab.chatgpt.com https://api.mapbox.com https://cdn.openai.com https://events.mapbox.com https://learn.chatgpt.com https://persistent.oaistatic.com https://production.plaid.com https://sandbox.plaid.com wss://chatgpt.com wss://ws.chatgpt-staging.com wss://ws.chatgpt.com;"
-    />
+    <base href="/" />
+    <script type="module" src="./assets/preload.js"></script>
     <meta charset="UTF-8" />
     <meta name="viewport" content="width=device-width, initial-scale=1.0" />
     <title>ChatGPT</title>
+    <link rel="icon" type="image/svg+xml" href="./favicon.svg" />
+    <link rel="manifest" href="/manifest.json" />
     <style>
       /* Dependency CSS can load before app.css, so establish Tailwind's layer order here. */
       @layer theme, base, components, utilities;
@@ -160,6 +160,30 @@
       crossorigin
       href="./assets/app-shared-6e7047a700cb.css"
     />
+    <style>
+      .main-surface {
+        --spacing-token-safe-header-left: 0px;
+      }
+
+      @media (max-width: 768px) {
+        body,
+        #root {
+          height: var(--codex-web-viewport-height, 100dvh);
+        }
+      }
+
+      @media (max-width: 768px) and (pointer: coarse) {
+        [data-app-action-sidebar-scroll] {
+          touch-action: pan-y;
+          overscroll-behavior-y: contain;
+          -webkit-overflow-scrolling: touch;
+        }
+
+        [data-app-action-sidebar-scroll] .touch-none {
+          touch-action: pan-y;
+        }
+      }
+    </style>
   </head>
   <body tabindex="0" style="outline: none">
     <div id="root">
