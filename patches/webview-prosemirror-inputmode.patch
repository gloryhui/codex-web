--- a/webview/assets/app-primary-6b28e06666ff.js
+++ b/webview/assets/app-primary-6b28e06666ff.js
@@ -85235,13 +85235,21 @@
               restoreRichLinks: !C,
             })
           : Jl({ schema: y, text: e }));
-  let j = new tDe(null, {
-      attributes: {
-        "aria-multiline": `true`,
-        dir: `auto`,
-        role: `textbox`,
-        spellcheck: `true`,
-      },
+  let inputModeDisabled = !1,
+    getAttributes = () => ({
+      "aria-multiline": `true`,
+      dir: `auto`,
+      role: `textbox`,
+      spellcheck: `true`,
+      ...(inputModeDisabled ? {} : { inputmode: `none` }),
+    }),
+    setPointerInputMode = (e) => {
+      inputModeDisabled !== e &&
+        ((inputModeDisabled = e),
+        j.isDestroyed || j.setProps({ attributes: getAttributes }));
+    },
+    j = new tDe(null, {
+      attributes: getAttributes,
       transformPastedHTML(e) {
         return e.length > IH ? ` ` : jde(nfe(e).body.innerHTML) || ` `;
       },
@@ -85368,6 +85376,15 @@
           b.dispatchEvent(new CustomEvent(dH, { detail: e.docChanged })));
       },
       handleDOMEvents: {
+        mousedown(e, t) {
+          return (setPointerInputMode(!0), !1);
+        },
+        touchstart(e, t) {
+          return (setPointerInputMode(!0), !1);
+        },
+        blur(e, t) {
+          return (setPointerInputMode(!1), !1);
+        },
         keyup(e, t) {
           let { $from: n, empty: r } = e.state.selection;
           return (
