--- a/webview/assets/local-conversation-thread-9a0a61c5e076.js
+++ b/webview/assets/local-conversation-thread-9a0a61c5e076.js
@@ -21525,6 +21525,11 @@
       startedAtMs: y?.submittedAtMs ?? null,
     }));
   b.conversationId === e && b.startedAtMs;
+  let codexHostedWindowTitle = Z(Lt, { hostId: n, threadId: e });
+  (0, YO.useEffect)(() => {
+    let title = codexHostedWindowTitle?.trim();
+    title && (document.title = `${title} | Codex`);
+  }, [codexHostedWindowTitle]);
   let x = no(pd),
     S = Z(vr, e),
     C = Ml(us).value,
